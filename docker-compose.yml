services:
  frontend:
    build:
      context: ./PCT_frontend
      dockerfile: Dockerfile
    container_name: pct-frontend
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
